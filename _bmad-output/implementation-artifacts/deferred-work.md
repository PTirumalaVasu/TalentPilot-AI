# Deferred Work Ledger

## Deferred from: code review of 1-1-project-structure-and-core-dependencies (2026-07-09)

- **JWT `role` claim accepts any string (no enum check); `JWT_EXPIRATION_HOURS` has no bounds check** [backend/app/core/security.py] — not required by Story 1.1's AC3; Story 1.3 ("Role & Identity Scoping on Every Request") already specs role-enum validation (403 on unrecognized role) at the FastAPI-dependency layer — make sure that story's implementation covers this, don't let it slip through as "already handled."
- **No `import-linter`/lint enforcement of the AD-1 single-owner module boundary** — module `repository.py`/`service.py` stubs are docstring comments only; nothing structurally prevents a future cross-module import. Worth a dedicated architecture-hardening story/task once more modules have real code to enforce against.
- **No lockfile/hash-pinning beyond top-level `==` pins in `backend/requirements.txt`** — transitive dependencies are unconstrained. Reasonable for a 5-week internal pilot; revisit (e.g. `pip-compile` with hashes) only if reproducibility becomes an actual problem.
- **No engine-disposal/lifespan hook to close the async DB engine on shutdown** [backend/app/main.py] — no live-connection bug exists yet since Story 1.7 is the first story to exercise a real DB connection; add a `lifespan` context manager calling `engine.dispose()` once real DB traffic exists.

## Deferred from: code review of 1-2-jwt-token-generation-and-session-model (2026-07-09)

- **Cookie `Max-Age` is tied to global `settings.JWT_EXPIRATION_HOURS`, independent of any per-token custom expiry** [backend/app/auth/service.py] — matches this story's AC2 exactly as specified; revisit only if a future story needs variable-expiry tokens (e.g. "remember me") paired with `set_session_cookie`.
- **`JWT_EXPIRATION_HOURS` has no bounds check** (0/negative produces a degenerate cookie `Max-Age`) [backend/app/core/config.py] — duplicate of the Story 1.1 deferred item above; still unaddressed.
- **Both 401 failure paths (missing cookie vs. invalid/expired token) collapse to the same generic `HTTP_ERROR` response code** [backend/app/auth/service.py] — a real API-design enhancement (per-exception-type error codes) but requires redesigning Story 1.1's whole error-contract scheme; out of scope for a single story.
- **No CSRF mitigation or discussion for the new cookie-based session mechanism** (`SameSite=Lax` reduces but doesn't eliminate exposure) [backend/app/auth/service.py] — no source document (PRD/architecture spine) currently specifies a CSRF requirement; flag at the architecture level before Story 1.4 (login) ships a real state-changing endpoint using this cookie.
- **`decode_access_token` uses zero clock-skew leeway** (`jwt.decode` supports a `leeway=` param) [backend/app/core/security.py] — Story 1.2's own AC1/Dev Notes explicitly say not to touch `core/security.py`; flag for whoever next modifies that file.

## Deferred from: code review of 1-3-role-and-identity-scoping-on-every-request (2026-07-09)

- **`AppException.__init__`'s parameter order (`status_code, error_code, message`) is unconventional** and risks a silent positional-arg mixup in a future call site (both `error_code` and `message` are plain `str`, so no type error would catch a swap) [backend/app/core/errors.py] — no live bug today since all current call sites use keyword args; revisit if this class gains more call sites or contributors unfamiliar with the convention.
- **No `import-linter`/lint enforcement that `get_current_user` is the sanctioned entry point for session/role logic** — duplicate of the Story 1.1 deferred AD-1-enforcement item above; still unaddressed, now with more real code (`auth/service.py`) to enforce against.
- **Note: the `AppException` mechanism this story introduced could close Story 1.2's "both 401 paths collapse to HTTP_ERROR" deferred item above** (same-generation duplicate, listed there), but this story deliberately did not retrofit it onto `get_current_token_payload`'s existing 401s — out of scope for a single story, revisit together if that item is ever picked up.

## Deferred from: code review of 1-4-login-endpoint-and-credential-store-mock-for-mvp (2026-07-09)

- **No `EmailStr`/format validation on `LoginRequest.email`** [backend/app/auth/schemas.py] — safe today (malformed email just 401s via the generic path, verified empirically), reasonable hardening item, not required by any AC.
- **No rate limiting/brute-force protection on `/login`** [backend/app/auth/router.py] — explicitly out of scope for a local-only MVP pilot per PRD Open Question 9's framing; **this is the state-changing endpoint the Story 1.2 CSRF deferred item above flagged watching for** — revisit both together if this ever becomes internet-facing or gains a real (non-mock) credential store.
- **No OpenAPI `responses=` documentation of the 401 failure shape** [backend/app/auth/router.py] — cheap docs polish, not blocking.
- **`authenticate()` doesn't set a custom `INVALID_CREDENTIALS` error code** (unlike `get_current_user`'s `INVALID_ROLE`) [backend/app/auth/service.py] — already explicitly decided against in Story 1.4's own Previous Story Intelligence (AC3 only requires a message, no code); same-generation duplicate of the Story 1.2 "generic HTTP_ERROR" item above.

## Deferred from: consolidated code review of Stories 1.2, 1.3, 1.4 (2026-07-09)

A full re-review across all three stories together (Blind Hunter, Edge Case Hunter, plus a dedicated cross-story consistency pass) found **no new bugs and no cross-story drift** — the cross-story consistency check explicitly confirmed claim-key alignment through the full login → JWT → dependency chain, correct layering (Story 1.2's `get_current_token_payload` doesn't touch role/user_id; Story 1.3's `get_current_user` composes on top; Story 1.4 uses both correctly), and that Story 1.3's repository-hard-scoping carve-out is still accurate (still stubs). Most findings from the two adversarial layers turned out to be duplicates of items already logged above, matches of the epic's own literal AC text (e.g. the `INVALID_ROLE` message echoing the claim value back — that's spec'd, not a bug), or premised on a production deployment this project doesn't have (architecture AR-15: local-only, no production target) — those are not re-listed here. Two items are genuinely new:

- **`SameSite=Lax` on the session cookie will block it on cross-origin requests once a real frontend exists on a different origin than the API** [backend/app/auth/service.py] — cannot be meaningfully tested or fixed yet since no frontend exists; `ALLOWED_ORIGINS` already carries a "fill in once frontend build/serve target is decided" placeholder from Story 1.1. Revisit together once frontend work begins — may need `SameSite=None; Secure` for a cross-origin split, or same-origin serving to sidestep the issue entirely.
- **Cookie `Max-Age` (`set_session_cookie`, Story 1.2) and the JWT's own `exp` claim (`create_access_token`, Story 1.1) are two independently-computed encodings of the same session lifetime**, with no shared-clock guarantee between them [backend/app/auth/service.py, backend/app/core/security.py] — negligible in practice (both computed microseconds apart within the same synchronous request path), but worth knowing if session-lifetime precision is ever tightened or these two calls end up separated across an async boundary.

Also explicitly re-confirmed as correct, not gaps: `get_current_user`/`request.state.current_user` are unused today because nothing exists yet to protect (`assignments`/`content`/`dashboard` routers are still stubs) — Story 1.6 and Epic 2-5 are where this gets wired in, not a regression; no logout endpoint exists because that's Story 1.5, the very next story in the backlog.

## Deferred from: code review of 1-5-sign-out-and-session-invalidation (2026-07-09)

- **Same-second token collision can violate AC4** [backend/app/auth/service.py, backend/app/core/security.py] — `create_access_token` encodes `iat`/`exp` at whole-second precision with no `jti`, so two logins for the same user within the same wall-clock second mint a byte-identical JWT; signing out one session silently revokes a concurrent sibling. Verified empirically by two independent review layers. **User decision (2026-07-09): accepted as a documented limitation, not fixed** — local single-process, five-user demo pilot; odds of two same-second logins by the same user are low and the impact (an unexpected re-login) is minor, while fixing it would require touching `core/security.py` against this story's own explicit "don't touch" scope note. Revisit if a `jti`/microsecond-precision claim is ever added to `core/security.py` for other reasons.
- **Unbounded, unauthenticated growth of `_revoked_tokens`** [backend/app/auth/service.py] — `logout()` has no auth gate (by design, AC3) and adds any cookie value verbatim to `_revoked_tokens` without checking it decodes as a real JWT; repeated calls with distinct garbage values grow the set forever with zero real sessions behind them. The story's own Dev Notes accept "unbounded-but-tiny... bounded by logout actions during a single dev/demo run, five users, no persistence," but that framing assumes only benign, low-volume use — it doesn't cover this unauthenticated-spam vector, nor does it cover ordinary-usage-driven growth (expired entries are never pruned even for legitimate sessions). Distinct from the existing "no rate limiting on `/login`" deferred item (credential brute-force vs. unauthenticated memory growth on a different, ungated endpoint) despite the surface similarity — revisit if this ever runs somewhere more exposed than a local single-process pilot.
- **Cookie attributes hand-duplicated between `set_session_cookie` and `logout`'s `delete_cookie` call** [backend/app/auth/service.py] — `key`, `path`, `httponly`, `secure`, `samesite` are independently written in both places with no shared source of truth; same category of drift risk as the Story 1.2 cookie-`Max-Age`/JWT-`exp` duplication item above. Low risk today (both are still small, easy to eyeball), but the same kind of thing that silently drifts if one side is edited later (e.g., adding `domain=`).
- **Third near-duplicate `_build_protected_test_app()` test helper** [backend/tests/test_logout.py] — this is now the third test file (`test_auth_service.py`, `test_current_user.py`, `test_logout.py`) with its own copy of "spin up an isolated app around one dependency," with no shared test-utils extraction across any of them. No functional impact; a follow-up test-hygiene cleanup once the pattern stabilizes.

## Deferred from: code review of 1-6-protected-endpoint-gate-no-flash-of-protected-content (2026-07-09)

- **Load-bearing "route added after `include_router()` already ran" claim has no permanent regression test** [backend/tests/test_protected_router_gate.py] — the story's Dev Notes empirically verified (via a throwaway standalone script, not committed to the repo) that a route added to a router *after* `app.include_router()` has already run still inherits the router-level gate on this installed FastAPI (0.139.0). The committed `_build_probe_app()` test actually covers a different order (route added, then included). Not a current correctness bug — this codebase always adds routes via decorators inside each module's own `router.py`, which execute at import time, before `main.py`'s `include_router()` calls run, so the order the test does cover matches how Epic 2–5 will add real routes. Revisit if this import pattern changes, or add a genuine "include first, add route after" regression test as cheap insurance for a future FastAPI upgrade.
- **Signature-tampering helper duplicated (again) from `test_auth_service.py`** [backend/tests/test_protected_router_gate.py:88-92] — same category as the Story 1.5 "third near-duplicate test helper" item above; this is now a fourth copy of similar token-crafting logic with no shared extraction. Bundle with that item in any future test-hygiene cleanup pass.

## Deferred from: code review of 1-7-database-schema-initialization-and-migration (2026-07-09)

- **ORM models still declare plain `sa.Enum(...)` without `create_type=False`** [backend/app/auth/models.py:17, backend/app/assignments/models.py] — stylistic drift from the migration, which now uses `postgresql.ENUM(..., create_type=False)` for the same 4 enum type names. Confirmed *not* an active bug: `MetaData.create_all()` (the path `models.py`'s enums would go through) dedupes type creation via a shared memo regardless of the flag. Worth aligning eventually for autogenerate hygiene/consistency; out of this diff's touched-file scope.
- **`conftest.py`'s session-scoped `test_engine` fixture builds schema via `Base.metadata.create_all()`/`drop_all()` against the same DB `alembic`/`settings.DATABASE_URL` target** [backend/tests/conftest.py] — currently unused by any test, so not an active bug, but a real landmine: if ever wired into a test, `drop_all()` at teardown would desync `alembic_version` (claims revision `001` applied while the tables it describes no longer exist). Revisit before this fixture is ever actually used.
- **No automated regression test proves the `DuplicateObjectError` double-`CREATE TYPE` fix persists against future migration edits** [backend/alembic/versions/] — the fix (all 5 enum columns using `create_type=False`) is currently verified only by a one-time live `alembic upgrade head` run this session, not a permanent CI guard. A future migration touching `role_enum`/`status_enum`/etc. with a plain `sa.Enum(...)` would silently reintroduce the bug undetected until someone runs the migration live again. Worth an automated test that runs `alembic upgrade head` against a scratch schema, once the effort to build that harness is justified.
- **`app.core.db.engine`'s connection pool is never disposed; the `loop_scope="module"` fix only covers `test_database_schema.py`** [backend/app/core/db.py, backend/tests/test_database_schema.py] — same category as the Story 1.1 deferred "no engine-disposal/lifespan hook" item (now more relevant, since real DB traffic exists as of this story). Any future test file that imports `engine` directly and forgets the same `pytest.mark.asyncio(loop_scope="module")` marker will reproduce the identical cross-event-loop asyncpg connection corruption this pass fixed here. No current reproduction path — confirmed no other test file uses `engine` directly today.
- **`test_timestamps_are_utc`'s assertion doesn't distinguish `TIMESTAMP WITH TIME ZONE` from `WITHOUT TIME ZONE`** [backend/tests/test_database_schema.py] — pre-existing assertion logic (`"TIMESTAMP" in type_str.upper()`), unchanged by this pass. AC9's timezone-awareness requirement isn't actually enforced by this test; a future regression to `sa.DateTime(timezone=False)` on any model would pass undetected.

## Deferred from: dev-story of 3-1-assignments-data-model-and-hr-admin-scope (2026-07-10)

- **The "engine pool never disposed" item above (Story 1.7 review) predicted the wrong failure mode — the real one is worse and was hit live, not just theorized.** That item assumed the risk was a future test file *forgetting* `pytest.mark.asyncio(loop_scope="module")`. This story added two new live-DB test files (`test_assignments_repository.py`, `test_assignments_service.py`) that both correctly used the marker, and the shared `app.core.db.engine` singleton still broke — for all three live-DB files, including the pre-existing `test_database_schema.py` — the moment they ran together in one `pytest` session. The actual bug: `engine`'s pooled connections bind to whichever module-scoped loop first uses them, and **any second module-scoped-loop test file that touches the same shared pool** corrupts it (`InterfaceError`, "another operation in progress"-class failure), regardless of whether the marker is present. **Worked around in this story** by giving each new test file its own private `create_async_engine(settings.DATABASE_URL)` (see both files' module docstrings) rather than importing the shared singleton — not a fix to the underlying gap, just avoidance. `test_database_schema.py` itself was left untouched (out of this story's file-change scope) and only works today because it happens to run without a private-engine peer corrupting it first in the current file order; that's fragile, not fixed. **A real fix needs one of:** an `engine.dispose()` call between test modules (a `pytest` session/module-scoped hook, not an async pytest-asyncio fixture — those were tried and rejected here, see below), or migrating all live-DB tests to the same private-engine-per-file pattern this story used, or a proper app-lifespan-scoped engine with disposal. Whoever picks this up should treat "single shared engine across multiple live-DB test files" as the actual root cause, not "someone forgot a marker."
- **A `pytest-asyncio` async-generator fixture (even one that's not the request-scoped `db_session`, just a bare per-test session fixture) is incompatible with `pytest.mark.asyncio(loop_scope="module")` in the installed `pytest-asyncio` version** [backend/tests/test_assignments_repository.py, test_assignments_service.py] — raises `AssertionError` at fixture setup (`super().setup()` in `pytest_asyncio/plugin.py`), both for a plain per-test fixture and for an `autouse=True` module-scoped teardown-only fixture (tried as a first attempt at fixing the engine-pool item above). Every live-DB test file in this codebase now works around it by opening sessions via a plain `@asynccontextmanager` helper called directly inside each test, not a pytest fixture. Not filed as a bug against the library — likely intentional in how `loop_scope` overrides fixture scoping — but worth knowing before attempting a fixture-based refactor of any live-DB test file.

## Deferred from: code review of 3-1-assignments-data-model-and-hr-admin-scope (2026-07-10)

- **`find_existing_assignment` (`backend/app/assignments/repository.py`) has no role/ownership scoping** — accepts an arbitrary `employee_id` with no caller-identity check, unlike its sibling `list_assignments_for_employee`. Not a live bug today (zero callers, built for Story 3.4's duplicate-detection flow on top of the already-`require_hr_admin`-gated create path), but binding guidance for whoever wires it up next: do not expose it on an EMPLOYEE-reachable path without a wrapping scope/ownership check, or it becomes a cross-employee data leak.
- **FK-violation `IntegrityError` (nonexistent `employee_id`/`skill_id`) is not caught in `create_assignment`** [backend/app/assignments/repository.py] — propagates to the generic 500 handler instead of a clean 4xx. No AC in Story 3.1 calls for referential validation, and no route wires this service to real (potentially bad) input yet — revisit when Story 3.4/3.5 adds the actual `POST /api/assignments` endpoint, since that's the first point untrusted `employee_id`/`skill_id` values reach this code.
- **Each new live-DB test file's private `create_async_engine(...)` (`test_assignments_repository.py`, `test_assignments_service.py`) is created at module-import time and never disposed** — compounds, rather than fixes, the `app.core.db.engine`-never-disposed item already logged above under "Deferred from: dev-story of 3-1-...". Each additional live-DB test file that copies this now-recommended private-engine pattern adds one more leaked pool per test session. Revisit together with that item — a session-scoped `pytest` hook disposing every engine (shared and private) at suite end would close both at once.

## Deferred from: code review of 2-2-embedding-model-integration-sentence-transformers (2026-07-10)

- **No truncation/length handling for text exceeding the embedding model's max sequence length** (`all-MiniLM-L6-v2`'s ~256 token limit) [backend/app/core/embedding.py] — `sentence-transformers` silently truncates input beyond that length with no warning or error. Not a live bug today, since `embed_text`'s only current caller (`core/seeds.py`) passes short skill names well under the limit; binding guidance for Story 2.3 (batch ingestion) and Story 2.4 (semantic matching), whose Content descriptions are far more likely to exceed it. Whoever adds those call sites should either log a truncation warning or pre-truncate/chunk longer text explicitly, not rely on the library's silent default.
